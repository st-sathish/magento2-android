<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ramveltrader" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".RamvelTraderApplication"
        android:theme="@style/AppTheme" >

        <activity android:name=".activity.splashscreen.SplashScreenActivity" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.login.LoginActivity"/>
        <activity android:name=".activity.signup.SignUpActivity"/>
        <activity android:name=".forgotpassword.ForgotPasswordActivity" />
        <activity android:name=".activity.otp.received.OtpReceiveActivity" />
        <activity android:name=".activity.otp.verify.OtpVerifyActivity" />
        <activity android:name=".activity.landingpage.LandingPageActivity" />

    </application>

</manifest>